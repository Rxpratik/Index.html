        function openPage() {
            var x = document.getElementById("search").value;

            if (x === "school bag") {
                window.open("/School-bag.html");
            }

            if (x === "traveling bag") {
                window.open("/index.html");
            }

        }
